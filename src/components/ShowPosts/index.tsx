import { auth } from "../../../auth";
import { getAllPosts } from "../../../data/createPost";
import { getUserByEmail } from "../../../data/user";

const ShowPosts = async () => {
   
    return ( 
        <div>
            
        </div>
     );
}
 
export default ShowPosts;